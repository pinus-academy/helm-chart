apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crm-chart.fullname" . }}-test-connection"
  labels:
    {{- include "crm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crm-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
